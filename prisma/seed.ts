/**
 * Seed entry point — placeholder for a later part.
 *
 * Will seed: admin user, demo categories, demo products, restaurant settings.
 * Requires models to exist first (schema.prisma is intentionally empty in
 * Part 1). Run with: pnpm db:seed   →   prisma db seed
 */
export {};
